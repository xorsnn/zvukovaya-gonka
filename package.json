{
  "name": "zvukovaya-gonka",
  "private": true,
  "version": "0.12.0",
  "type": "module",
  "description": "Гонка звуков — голосовая игра для запуска речи у детей с задержкой речевого развития",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "gen-fixtures": "node scripts/gen-detection-fixtures.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.8"
  }
}
